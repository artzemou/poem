{
  "name": "poem",
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "start": "pm2-runtime start ecosystem.config.cjs --env production",
    "dev": "nodemon --watch server.js --watch app.js --ext js --exec 'node server.js'",
    "test": "jest"
  },
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "python-shell": "^5.0.0",
    "serve-favicon": "^2.5.1"
  },
  "devDependencies": {
    "connect-livereload": "^0.6.1",
    "livereload": "^0.10.3",
    "nodemon": "^3.1.10",
    "jest": "^29.4.0",
    "supertest": "^6.3.3"
  }
}
